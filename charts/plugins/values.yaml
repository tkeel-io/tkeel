# > tkeel platform values
# default plugin id
daprConfig: plugins
# < tkeel platform values


# plugins custom values
middleware:
  name: tkeel-middleware
replicaCount: 1
appPort: 8080
secret: changeme
image:
  repository: tkeelio/plugins
  tag: 0.0.1
  pullPolicy: Always